import EditProjectForm from "../../components/main/editProjectForm";

const Edit = () => {
  // const { id } = useParams();
  // id로 받아온 프로젝트 정보를 불러와서 form에 넣어줘야함
  // 이 데이터를 props로 넘겨주면 될듯
  return <EditProjectForm />;
};

export default Edit;
